name: no-parallelism-apple-clang
on: [push]

jobs:
  job:
    name: ${{ matrix.buildType }}
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        buildType: [Release]

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      
      - id: build
        uses: ./.github/actions/cmake-build
        with:
          cxx: clang++
          cc: clang
          triplet: x64-osx
          generator: Ninja
          buildDir: ${{ runner.workspace }}/b/${{ matrix.buildType }}
          cmakeArgs: -DBUILD_SAMPLES=ON -DBUILD_SAMPLES_SIMD=ON -DBUILD_TESTS=ON -DENABLE_PARALLELISM=OFF

      - name: Run CTest
        run: |
         cd ${{ runner.workspace }}/b/${{ matrix.buildType }}/test
         ctest -V
